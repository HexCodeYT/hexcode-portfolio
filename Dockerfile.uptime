FROM node:22-bookworm-slim

WORKDIR /app

COPY package.json package-lock.json ./
RUN npm ci

COPY lib ./lib
COPY scripts ./scripts
COPY tsconfig.json ./

ENV NODE_ENV=production
ENV PORT=3100

EXPOSE 3100

CMD ["npm", "run", "serve:uptime"]
